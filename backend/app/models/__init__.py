# Domain models are defined per-service module.
# Import them here to ensure Alembic detects all tables via Base.metadata.
#
# Example (add as services are built):
#   from app.models.user import User  # noqa: F401
